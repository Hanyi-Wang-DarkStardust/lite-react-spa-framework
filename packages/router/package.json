{
  "name": "@lite-react-spa/router",
  "version": "0.0.1",
  "description": "A HOC for router guards of React App",
  "main": "dist/index.js",
  "module": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "jest",
    "clean": "rimraf lib dist",
    "clean-all": "rimraf lib dist node_modules",
    "build": "rollup -c && tsc -p tsconfig.json",
    "prebuild": "rm -rf dist lib",
    "lint": "eslint src --ext \".ts,.tsx\"",
    "lint:fix": "eslint src --fix --ext \".ts,.tsx\""
  },
  "files": [
    "dist/",
    "lib/",
    "src",
    "dist"
  ],
  "sideEffects": false,
  "author": "Hanyi Wang",
  "license": "MIT",
  "dependencies": {
    "react-router-dom": "^6.3.0",
    "history": "^5.3.0"
  },
  "peerDependencies": {
    "react": "^16.8.0 || 17.x || 18.x",
    "react-dom": "^16.8.0 || 17.x || 18.x"
  }
}
